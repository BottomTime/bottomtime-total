extends layout.pug

block content
  p.content.block
    | Hi there #{user.displayName},

  p.content.block
    | You are receiving this email because you requested a link to verify your email address with
    | the #[a(href!=baseUrl) Bottom Time] application for social scuba diving! Click the link below
    | to complete the request!

  div.block.box.has-text-centered
    span
      a(href!=verifyEmailUrl) Verify Your Email Address
      | #{" (" + user.email + ")"}

  p.content.block
    | Cheers,
    br
    | The Bottom Time Team!

  p.content.block.help
    | If you did not request this email and suspect something fishy is up then you can report this email
    | to #[a(href=`mailto:${adminEmail}`)= adminEmail].
