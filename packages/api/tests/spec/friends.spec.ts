describe('Friends API client', () => {
  it('will list friends', async () => {});

  it('will list friend requests', async () => {});

  it('will create a friend request', async () => {});

  it('will wrap a friend DTO', () => {});

  it('will wrap a friend request DTO', () => {});
});
