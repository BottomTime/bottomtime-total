{
  "name": "@bottomtime/docs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "docker:build": "docker build -t bottomtime/docs .",
    "docker:run": "docker run -it -p 3200:3200 --name bt-docs bottomtime/docs",
    "lint": "npx @redocly/cli lint public/specs.yml"
  },
  "devDependencies": {
    "@redocly/cli": "^1.0.0-beta.125"
  }
}
