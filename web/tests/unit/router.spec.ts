import router from '@/router';

describe('Router', () => {
  it('Will return a router', () => {
    expect(router).toBeDefined();
  });

  it.todo(
    'Do I need to write tests for this? I should assess that the routes work.',
  );
});
